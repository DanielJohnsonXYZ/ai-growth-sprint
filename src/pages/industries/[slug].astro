---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getUseCase, industries, SITE } from "../../data/site";

export function getStaticPaths() {
  return industries.map((industry) => ({
    params: { slug: industry.slug },
    props: { industry },
  }));
}

const { industry } = Astro.props;
const relatedUseCases = industry.relatedUseCases.map((slug: string) => getUseCase(slug)).filter(Boolean);
const schema = {
  "@type": "Service",
  name: `AI training for ${industry.title}`,
  provider: { "@type": "Organization", name: SITE.name, url: SITE.url },
  areaServed: "United Kingdom",
  serviceType: "AI workflow training",
};
---

<BaseLayout title={industry.metaTitle} description={industry.metaDescription} schema={schema}>
  <section class="page-hero">
    <div class="page-copy">
      <p class="eyebrow">AI training for {industry.shortName.toLowerCase()}</p>
      <h1>AI Training for {industry.title} - {industry.hero}</h1>
      <p>{industry.subhero}</p>
      <div class="hero-actions">
        <a class="button" href="/contact/">Book a free 15-minute call</a>
        <a class="button button-secondary" href="/how-it-works/">How it works</a>
      </div>
    </div>
    <aside class="panel panel-pad">
      <p class="eyebrow">Why {industry.shortName.toLowerCase()}</p>
      <h2>A sector built for this training.</h2>
      <p class="lead">{industry.stat}</p>
    </aside>
  </section>

  <section class="section section-tight">
    <div class="grid grid-3">
      <article class="card card-featured">
        <p class="eyebrow">Who it is for</p>
        <h3>{industry.audience}</h3>
        <p>{industry.size}</p>
      </article>
      <article class="card">
        <p class="eyebrow">Results</p>
        <h3>Proof collection in progress</h3>
        <p>Named testimonials and case studies will be added as they are approved. No invented proof.</p>
      </article>
      <article class="card">
        <p class="eyebrow">Next step</p>
        <h3>Book a free 15-minute call</h3>
        <p>Tell us what your team spends too long on. We will map the first workflows worth automating.</p>
      </article>
    </div>
  </section>

  <section class="section">
    <div class="section-header">
      <p class="eyebrow">5 workflows</p>
      <h2>Where AI can speed up the work.</h2>
      <p>
        Each workflow is designed as a draft-and-review process. AI helps create
        the first version. The team stays responsible for facts, judgment, and sign-off.
      </p>
    </div>
    <div class="workflow">
      {industry.workflows.map((workflow) => (
        <article class="workflow-row">
          <div>
            <p class="eyebrow">Task</p>
            <strong>{workflow.name}</strong>
          </div>
          <div>
            <p class="eyebrow">Before</p>
            <p>{workflow.now}</p>
          </div>
          <div>
            <p class="eyebrow">After</p>
            <p>{workflow.withAi}</p>
          </div>
          <div>
            <p class="eyebrow">Time saved</p>
            <p>{workflow.timeSaved}</p>
          </div>
        </article>
      ))}
    </div>
  </section>

  <section class="section section-tight">
    <div class="split">
      <div class="panel panel-pad">
        <p class="eyebrow">Trust block</p>
        <h2>Safe use for {industry.shortName}.</h2>
        <p class="lead">{industry.trustNote}</p>
        <a class="button" style="margin-top: 24px" href="/trust-security/">Read trust and security</a>
      </div>
      <div>
        <div class="section-header">
          <p class="eyebrow">Sector insights</p>
          <h2>What makes {industry.shortName.toLowerCase()} different.</h2>
        </div>
        <div class="grid">
          {industry.insights.map((insight) => (
            <article class="card">
              <strong>{insight}</strong>
            </article>
          ))}
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="section-header">
      <p class="eyebrow">Related use cases</p>
      <h2>Same workflows, connected across sectors.</h2>
    </div>
    <div class="grid grid-3">
      {relatedUseCases.map((useCase) => (
        <article class="card">
          <h3>{useCase?.title}</h3>
          <p>{useCase?.problem}</p>
          <a href={`/use-cases/${useCase?.slug}/`}>View use case</a>
        </article>
      ))}
    </div>
  </section>

  <section class="section section-tight">
    <div class="panel panel-pad">
      <p class="eyebrow">Next step</p>
      <h2>Want to see which {industry.shortName.toLowerCase()} workflows AI can speed up first?</h2>
      <p class="lead">Book a short call. Bring the tasks your team spends too much time on.</p>
      <a class="button" style="margin-top: 24px" href="/contact/">Book a free 15-minute call</a>
    </div>
  </section>
</BaseLayout>
