---
import BaseLayout from "../layouts/BaseLayout.astro";
import { industries, SITE } from "../data/site";
---

<BaseLayout
  title="Contact"
  description="Book a free 15-minute call with AI Growth Sprint."
>
  <section class="page-hero">
    <div class="page-copy">
      <p class="eyebrow">Contact</p>
      <h1>Book a free 15-minute call.</h1>
      <p>
        Tell us where AI feels useful but risky. We will map the first safe
        workflows, the approval rules you need, and whether a workshop makes sense.
      </p>
      <div class="button-row">
        <a class="button" href="#quick-enquiry">Use quick enquiry form</a>
        <a class="button button-secondary" href={`mailto:${SITE.email}`}>{SITE.email}</a>
      </div>
    </div>
    <form id="quick-enquiry" class="panel panel-pad" name="contact" method="POST" data-netlify="true">
      <input type="hidden" name="form-name" value="contact" />
      <p class="eyebrow">Quick enquiry</p>
      <label>
        Name
        <input name="name" autocomplete="name" required />
      </label>
      <label>
        Email
        <input name="email" type="email" autocomplete="email" required />
      </label>
      <label>
        Company
        <input name="company" autocomplete="organization" />
      </label>
      <label>
        Industry
        <select name="industry" required>
          <option value="">Choose one</option>
          {industries.map((industry) => <option value={industry.slug}>{industry.title}</option>)}
        </select>
      </label>
      <label>
        Team size
        <input name="team-size" />
      </label>
      <label>
        Where could AI help, and what would you be nervous about?
        <textarea name="time-drain" rows="5"></textarea>
      </label>
      <button class="button" type="submit">Send enquiry</button>
    </form>
  </section>
</BaseLayout>

<style>
  form {
    display: grid;
    gap: 16px;
  }

  label {
    display: grid;
    gap: 8px;
    color: var(--muted);
    font-weight: 700;
  }

  input,
  select,
  textarea {
    width: 100%;
    border: 2px solid var(--ink);
    border-radius: 16px;
    background: var(--paper-soft);
    color: var(--ink);
    font: inherit;
    padding: 12px 14px;
  }

  input:focus,
  select:focus,
  textarea:focus {
    outline: 3px solid var(--amber);
    outline-offset: 2px;
  }

  button {
    cursor: pointer;
  }
</style>
